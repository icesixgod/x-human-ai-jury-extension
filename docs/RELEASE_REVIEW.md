# v0.4.2 评论详情修复审查

2026-09-25：修复单条评论详情页跳过当前评论的问题。仅从该页面同一对话区域中此前展示的原帖与上级回复构造上下文，不发起 X 抓取请求。父帖被截断时显示“评审 · 打开原帖”，不调用 Jev 或社区接口；受保护上下文直接跳过。

回归覆盖当前评论、祖先与引用的区分，完整与截断上下文，展开后恢复，虚拟滚动，页面跳转和请求不外发。类型检查和 45 项测试通过。默认 API 保持 https://x.aileetcode.com，权限、个人 Key 数据流和服务端私有范围不变。发布仍须通过源码、全部可达历史、归档、依赖审计和独立重建门禁。

以下保留首次独立发布的审查记录。

# v0.4.1 发布审查

范围：浏览器插件独立仓库、全部可达提交、独立构建和公开附件。网站、后台、服务端、数据库和私有部署配置不纳入开源范围。

发布门禁：

- 独立建立 Git 历史；仅复制明确选择的插件源文件、客户端类型、合成测试及许可证。
- `.gitignore` 排除环境文件、密钥、身份数据、依赖、构建、日志、数据库和私有平台目录；发布白名单再次拒绝未授权路径，即使强制添加也会失败。
- 检查 Key 隔离、消息伪造、任意请求目标、HTML 注入、个人模型不上传、改票/撤票/不确定票与离线重启同步。
- 类型检查、单元测试、生产构建、npm 高危依赖审计、全历史和嵌套压缩包扫描。
- 从源码包在临时目录全新安装依赖，逐字节比较所有构建输出。
- 发布 ZIP 与源码对应干净提交，提供 SHA256SUMS、SBOM、准确提交链接及许可证。

本次审查于 2026-09-25 执行：39 项插件与发布边界测试通过，类型检查通过，依赖审计报告 0 个已知漏洞。对异常社区计数、序列和跨评论响应增加了校验与回归测试，防止污染缓存或本地投票队列。

此版本修复了原完整应用打包范围过宽的问题：新安装包及新源码包只包含插件。没有继承原应用历史，也没有发布后台管理、服务器实现、数据库内容、私密地址或凭据。

审查不能证明不存在未知漏洞，也不验证模型识别准确率。真实 X DOM 随时可能变化；自动测试采用合成页面，没有向真实帖子投票。服务端不属于此公开仓库的安全审计范围。此前已对外提供过的完整应用版本不在本次新仓库的历史中，历史接收者已获许可不因拆分而撤回。

每次发布必须重新运行 README 中的检查，不能将本记录替代后续审查。

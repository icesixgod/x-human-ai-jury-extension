# 安全

支持最新正式版插件。请通过 [GitHub 私密漏洞报告](https://github.com/icesixgod/x-human-ai-jury-extension/security/advisories/new) 向维护者报告安全问题；不要在公开 Issue 中贴出凭据、可识别的用户数据或可直接滥用的细节。报告应包含版本、影响、最小复现和建议修复。没有真实数据时请使用合成样本。

安全边界：

- Content script 不读取 Key；session/local 存储限定为可信扩展上下文。默认只保存当前会话，选择记住才写本地；本地存储不提供静态加密。
- 后台校验扩展 ID、顶层 X 帖子页面来源、消息类型和字段，拒绝页面提供的网络目标。
- API 目标在构建时固定。请求省略 Cookie、拒绝重定向，TypeSafe Key 只发送到固定 TypeSafe API。
- 不加载远程可执行代码；Manifest V3 CSP 和最小权限由发布门禁检查。纯文本渲染不执行评论中的 HTML。
- 安装包和对应源码由文件白名单构建；扫描完整 Git 历史及嵌套压缩包。扫描失败不得发布。

自动检查无法证明没有未知漏洞。账户、浏览器、TypeSafe 或评审服务被攻破不在插件单独能解决的范围。服务端不属于此仓库的审计范围，不应将插件审查解释为对服务器的独立审计。
